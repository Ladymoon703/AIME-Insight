# AIME Insight 口述稿（3–5 分钟）

## 1. 用户问题

一个关注某家上市公司的普通个人投资者，真正面对的不是「信息太少」，而是「信息太碎」。

他要同时看行情、财报、估值、公告、新闻、行业、政策……但把这些拼成一个能验证、能被质疑、还能随着新数据更新的判断，非常困难。

更糟的是，市面上大多数工具要么是新闻瀑布流，要么是「AI 直接告诉你买不买」的股票问答或评分器——它们都跳过了「研究」这个过程本身。

## 2. 产品方案

AIME Insight 是「AI 证据驱动的公司研究工作台」。

用户提出一个研究问题，比如「为什么贵州茅台最近股价表现较弱」。系统不是直接给一句结论，而是：

先生成 Research Map（研究计划），告诉用户它准备从行情、财务、估值、行业、事件几个维度去研究，用户可以增删维度、改时间范围。

然后去拿真实金融数据，用确定性引擎算指标，把每个结论都落到一条可追溯的 Evidence 上，再用 DeepSeek 去解释这些证据。

最后，用户可以把研究保存下来、建立观察，下次重新研究时，系统会对比新旧证据，告诉他「你的判断被强化了，还是被削弱了」。

## 3. 核心差异

它不是 ChatGPT + 股票，因为 LLM 不负责算数、不负责给数据，只负责解释已经存在的证据。

它不是新闻聚合，因为首页是研究入口，不是资讯流。

它不是 AI 诊股或评分器，因为系统不给「85 分」「强烈看好」，只给「改善 / 承压 / 分化 / 待验证」这样的研究状态，并且明确区分事实、推断和无法验证。

## 4. 核心技术

核心链路是：

Research Map → Data Adapter → Deterministic Metric Engine → Evidence Engine → LLM Interpreter → Research State → Observation。

其中数据通过扶摇金融数据 API 获取；同比、回撤、波动率这些金融指标全部由确定性引擎计算；证据引擎把「结论 → 指标 → 期次 → 来源 → 口径」串起来；LLM 只做解释。

## 5. AI 如何避免胡编

这是我最想强调的一点：**LLM 负责解释 Evidence，不负责创造 Evidence。**

LLM 拿到的输入只有已经生成的 Evidence 对象。它的输出必须是结构化 JSON，并且每个事实性陈述都要引用 Evidence ID。

系统做了三道校验：结构校验、Evidence ID 校验（引用不存在的 ID 就拒绝）、数字溯源校验（出现 Evidence 里没有的金融数字就拒绝）。任何一步失败，就降级到模板解释，绝不让非法输出进入页面。

所以用户看到的每一个数字，都能在 Evidence Drawer 里找到原始字段和来源。

## 6. 为什么 Research Memory 有价值

一次分析不是终点。

研究应该能够：保存 → 重新研究 → 比较变化 → 建立观察 → 继续更新。

AIME Insight 把每次研究保存为不可变的版本快照。下次重新研究，系统做确定性比较，告诉你：哪些判断被强化、哪些被削弱、新增了什么矛盾、出现了什么未知、哪些旧证据失效了。同一家公司还能有多个研究任务（按研究目标区分），互不干扰。

这样，研究就从一个「一次性的问答」变成了「一个持续更新的过程」。

## 7. 当前限制

我不回避边界：

当前没有历史 PE 序列（扶摇只提供最新估值快照），所以不展示历史估值曲线；公告和新闻依赖 iFinD MCP，未接入时就诚实显示「暂无数据」；没有后台定时任务；实时行情不保证持续可用。

宁可不显示，也不伪造。

## 8. Demo 收尾

所以最终，系统不会说「AI 告诉你该买还是卖」。

它做的是：让研究过程更结构化、可验证、可持续。

**最终判断仍然由用户自己完成，系统负责让他的研究有证据、能追溯、能持续。**
