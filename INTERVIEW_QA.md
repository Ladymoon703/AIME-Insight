# AIME Insight 面试问答

以下回答全部基于当前真实实现，不含虚构功能。

## 1. 为什么做这个产品？

普通投资者研究一家公司时，最难的不是「找不到信息」，而是「无法把碎片信息组织成一个能验证、能更新、能被质疑的判断」。市面上的工具要么是新闻流，要么是「AI 直接给结论」的问答/评分器，都跳过了「研究」本身。AIME Insight 想把「研究过程」做成一等公民。

## 2. 用户是谁？

有明确关注标的、有基本投资知识（懂营收/净利润/PE/ROE）、希望自己做判断但不想手动拼信息的个人投资者。次级用户是金融经验较少、更依赖图表和分层解释的普通投资者。

## 3. 用户痛点是什么？

三个断裂：数据→判断（看到一堆数字却不知道它们的关系）、证据→结论（看到「盈利改善」却不知道依据和反面证据）、一次判断→持续研究（今天分析完，下次财报变了不知道原判断是否还成立）。

## 4. 为什么不是普通聊天机器人？

聊天机器人是「Search → Summarize → Finish」，一次性问答。AIME 是「Research → Validate → Track → Update」，是持续研究。而且聊天机器人容易让 LLM 直接给数字，AIME 强制 LLM 只能解释已有的 Evidence。

## 5. 为什么需要 Research Map？

研究一个模糊问题（「为什么股价弱」）需要先拆解成可研究的维度（行情/财务/估值/行业/事件）。Research Map 让 AI 主动规划，同时让用户能增删维度、改优先级、改时间窗——既透明，又可控，避免把用户直接扔进数据看板。

## 6. 为什么需要 Evidence？

因为「AI 说盈利改善」没有说服力，用户需要知道「为什么、依据是什么、有没有反面证据、哪部分是事实哪部分是推断」。Evidence 把结论和「指标/期次/来源/口径/原始字段」绑定，形成可追溯的闭环。

## 7. 为什么 LLM 不直接分析数据？

金融指标（同比/回撤/波动率等）必须可复现、可测试、可追溯。LLM 会算错、会编数。所以确定性引擎负责算数，LLM 只负责理解、解释、发现问题、提出下一步。一句话：**LLM 负责解释 Evidence，不负责创造 Evidence。**

## 8. 如何防止金融数据幻觉？

三层：① LLM 输入只有 Evidence，系统提示明确禁止编数字；② 输出必须结构化并引用 Evidence ID；③ 三道校验——JSON/schema 校验、Evidence ID 校验（引用不存在就拒绝）、数字溯源校验（出现 Evidence 里没有的带单位数字就拒绝）。任一失败降级到模板解释。

## 9. Evidence 和 LLM 的关系？

Evidence 是「事实载体」，LLM 是「解释器」。LLM 只能引用 Evidence，不能生成新的 Evidence。页面上从「AI 一句话 → Evidence → 原始字段」可以完整钻取。

## 10. Research State 是什么？

一次研究的完整结构化快照：研究目标、维度状态（改善/承压/分化/待验证）、核心结论、正/负/矛盾/未知证据、下一步问题、时间戳。它被保存为不可变版本，支持「旧 vs 新」比较。

## 11. Observation 为什么不是简单提醒？

Observation 不是「自选股提醒」，而是「我希望未来继续验证的研究问题」。它绑定到具体 Research（researchId），保存观察指标，用户主动点击「检查最新情况」时，系统重新研究并做确定性比较，告诉你判断被强化/削弱/新增矛盾/新增未知——而不是简单推送一个价格变动。

## 12. 为什么没有历史 PE？

扶摇当前只提供最新估值快照（PE-TTM/PB-MRQ 等 5 个字段），没有历史估值序列。所以不展示历史 PE 曲线和分位，改为「当前估值 + 行业比较 + 盈利趋势」，并明确标注「历史估值序列不可用」。宁可缺失，不伪造。

## 13. 为什么没有新闻？

公告/新闻/研报走 iFinD MCP，需要浏览器 Cookie 鉴权，MVP 未接入。所以事件维度在 live 模式诚实显示「暂无事件数据/公告新闻未接入」，不伪造新闻。

## 14. 如果接入 iFinD，产品下一步怎么扩展？

事件/公告/新闻/研报/政策就能真实接入，事件时间线和「事件对研究判断的影响」能补全。数据层已有 Data Adapter 抽象，接入 iFinD 只新增一个 adapter，不影响上层。同时可补充同行区间收益、股票→行业成分反查（当前扶摇「敬请期待」）。

## 15. 如何判断这个产品是否有效？

看用户是否从「一次性问答」转向「持续研究」：是否保存研究、是否建立观察、是否回来重新研究并查看变化。核心不是生成多少文字，而是用户是否沿着 Research Loop 继续走下去。

## 16. 最核心的产品指标是什么？

Evidence 可追溯率（核心结论能追溯到数据的比例）、无证据支撑结论率（越低越好）、数据失败透明率（缺失/失败是否正确暴露）、研究延续率（是否继续研究）、研究保存率。其中「无证据支撑结论率」最能体现 AI grounding 是否成立。

## 17. 哪个功能最值得继续迭代？

Research Update（新旧研究比较）和 Observation（持续观察）。因为它们是产品从「一次性工具」变成「持续研究 Agent」的关键。其次是 Evidence 溯源体验的深入（钻取到原始数据）。

## 18. 这个产品有哪些风险？

① 数据源可用性风险（扶摇/iFinD 接口变动或限流）；② LLM 输出质量风险（即便有校验，仍需持续对抗幻觉）；③ 合规风险（必须严守不输出买卖建议/涨跌预测）；④ 用户信任风险（数据缺失时若表达不清，用户可能误判）；⑤ 竞品风险（同花顺自身有更全的数据和分发）。

## 19. 如果用户不懂金融，产品怎么帮助他？

通过 Visual First：先看图表和数字卡（降低认知门槛），配一句话 AI 观察（用白话解释关键关系），再分层展开深度分析。同时明确区分「事实/推断/无法验证」，让用户知道哪些是确定的、哪些是 AI 的推断、哪些还没有数据。

## 20. 如果用户是有经验的投资者，产品还能提供什么？

提供可追溯的证据链（每个结论能回到原始字段和口径）、多维背离检测（基本面/现金流/行情/估值不一致）、确定性指标（回撤/波动率等）、以及持续的研究版本比较——这些是「看数据」之外的「验证和更新」能力。

## 补充：产品的诚实边界

系统明确不提供：买卖建议、股票评分、涨跌预测、收益承诺、自动交易、定时推送。凡是当前没实现的能力（历史 PE、新闻、后台 scheduler），一律如实标注，绝不为了演示伪造。
