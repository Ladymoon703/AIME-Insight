# ===== AIME Insight 环境变量模板 =====
# 复制本文件为 .env.local 并填写真实值（.env.local 已被 gitignore，不会提交）。

# 扶摇金融数据 API Key（https://fuyao.aicubes.cn/admin 签发）
# 未配置时，数据层自动进入「演示数据」模式，所有数据明确标注为演示数据，绝不伪装成真实数据。
FUYAO_API_KEY=

# DeepSeek（OpenAI 兼容）用于 Agent 的 LLM 解释层
# 未配置时，Agent 使用确定性规则引擎 + 模板文本兜底，不生成未经数据支撑的内容。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 本地 SQLite 数据文件路径（可选，默认 data/aime.db）
AIME_DB_PATH=

# Turso 持久化（配置后优先走 Turso；不配置则用本地 file: SQLite）
# Vercel 上务必配置，否则保存的研究/观察会因 /tmp 不持久而丢失
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
